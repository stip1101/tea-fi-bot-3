/**
 * Global test setup for TeaFi Bot
 *
 * This file can be used for global test configuration.
 * Individual test files handle their own beforeEach cleanup
 * by calling reset*Factory() functions.
 *
 * To use as a preload file:
 * bun test --preload ./tests/setup.ts
 */

// Global test configuration can be added here
// For example: setting up global mocks, environment variables, etc.

// Note: Factory resets are handled in individual test files
// using beforeEach(() => resetUserFactory()) pattern
